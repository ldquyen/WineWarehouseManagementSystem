@page
@model WineWarehouseManagementSystem.Pages.CategoryPages.DeleteModel
@{
    Layout = "_ManagerLayout";
    int? accountId = HttpContext.Session.GetInt32("accountId");
    int? roleId = HttpContext.Session.GetInt32("roleId");
    if (accountId == null || roleId != 2)
    {
        TempData["ErrorMessage"] = "This function is for manager only";
        Response.Redirect("/Error");
    }
}

<h1 style="color:white; text-align:center">Delete category</h1>
@if (TempData["Message"] != null)
{
    <h2 style="text-align: center; color: #4CAF50;">@TempData["Message"]</h2>
}
<form method="post" style="max-width: 500px; margin: 20px auto; padding: 20px; border: 1px solid #ccc; border-radius: 8px; background-color: white">
    <div style="margin-bottom: 15px; display: flex; flex-direction: column;">
        <label asp-for="category.CategoryId" style="margin-bottom: 5px; font-weight: bold;">Category ID</label>
        <input asp-for="category.CategoryId" value="@Model.category.CategoryId" readonly
               style="padding: 8px; border: 1px solid #ccc; border-radius: 4px; background-color: #e9ecef;" />
    </div>

    <div style="margin-bottom: 15px; display: flex; flex-direction: column;">
        <label asp-for="category.CategoryName" style="margin-bottom: 5px; font-weight: bold;">Category Name</label>
        <input asp-for="category.CategoryName" value="@Model.category.CategoryName" readonly
               style="padding: 8px; border: 1px solid #ccc; border-radius: 4px;" />
    </div>

    <div style="text-align: center;">
        <button type="submit"
                style="padding: 10px 20px; background-color: red; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 16px;">
            Delete Category
        </button>
    </div>
</form>